email = 'CONTACT@MINNEAPOLIS.EDU'

email_lowercase = email.lower()  # function to make it all lowercase very conveniently.

print(email_lowercase)  # could also reuse the same variable if you want. reassign it the new value.
