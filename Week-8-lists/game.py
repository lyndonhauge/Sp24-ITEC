"""
can randomly pick from a list using the random function along with choice
"""

import random

options = ['rock', 'paper', 'scissors']
computer_choice = random.choice(options)
print(computer_choice)
